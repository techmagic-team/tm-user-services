// Mutations for Shared //
const mutations = {
  setDrawer(state, drawer) {
    state.drawer = drawer;
  },

  setNotification(state, notification) {
    state.notification = notification;
  },
};

export default mutations;
